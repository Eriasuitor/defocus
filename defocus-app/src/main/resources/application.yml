spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/postman?charset=utf8mb4&serverTimezone=UTC
        username: root
        password: ''
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
server:
  port: 10086